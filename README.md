# forYOU

Open `confession.html` in a browser to view the Confession card.
To serve locally (recommended):

```powershell
python -m http.server 8000

# then open http://localhost:8000/confession.html
```